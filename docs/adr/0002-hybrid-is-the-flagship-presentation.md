# Hybrid is the flagship presentation

Blendlink treats a hybrid Web Presentation as the flagship workflow: static environment objects may be baked while moving, deforming, transparent, effect-driven, or deliberately live objects remain dynamic. Pure realtime and pure baked scenes are simpler variants; arbitrary Blender render parity is not promised, so unsupported features must be diagnosed before export rather than silently degraded.
